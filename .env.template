# GRT doesn't require a key but the ttc and octranspo APIs do.
# This file is a template for the .env file that should be created with the actual keys.
octranspo=OCTRANSPO_API_KEY
ttc=TTC_API_KEY

# Override the hostname each agency is served on (defaults shown).
# These must match what HAProxy routes to this server.
# GRT_HOST=grt.jskw.ca
# TTC_HOST=ttc.jskw.ca
# OCT_HOST=oct.jskw.ca
